# Eval strength

Row vs column eval. Colors alternate each game. Score = (row wins + 0.5×draws) / games × 100.

## 2026/06/11 22:20:52 — depth=8 games=10 parallel=thread-pool

```
depth=8 games=10 parallel=thread-pool
win_rate_pct (row vs col; colors alternate each game)
          component     count      mask
component         -     100.0     100.0
    count       0.0         -       0.0
     mask       0.0     100.0         -
```
